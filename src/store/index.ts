import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    parentUsername: "admin",
    parentPassword: "admin",
    zalogowano: false,
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
